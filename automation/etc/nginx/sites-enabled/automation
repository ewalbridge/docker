server {
    listen 8888;

    server_name automation.moodybridge.local;
        
    location /automation {
        proxy_pass automation.moodybridge.local:5678;
        include proxy_params;
    }
}